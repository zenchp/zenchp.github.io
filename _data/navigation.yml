pages:
# NOTE: `name` field must match the `navbar_title` field in the page's front matter to ensure the correct page is highlighted in the navbar.
#       For example, the `navbar_title` field in `showcase.html` is "Showcase" and the `name` field here should also be "Showcase"
- name: Home
  url: /
- name: Publications
  url: /publications
- name: Blog
  url: /blog
# - name: Showcase
#   url: /showcase This can be uncommented when the showcase page is ready to be published. The `navbar_title` field in `showcase.html` should also be set to "Showcase" to ensure the correct page is highlighted in the navbar.
